revise
- for Jade
-- Raw Data visualiztion (in exp/outputs/results/raw_xxx.png
-- Table 1:  Maximum transmission distance ...-> just present the avg dist for all env

- for Julia
--  measure tool (screen shot)


- for Zwee
-- unicast(30 communication (after connection)) / broadcast (at the beginning (dist measurement) is connect or not , scan)


- for Timon (in conclusion part)
-- the observation of seq length? (in exp/outputs/results/heatmap_acc_resnet_env.png)
-- should we mention it?`splitbytime` performs slightly worse than random split; with the same configuration (`node_seq100_ov50_random_cnn`), accuracy is `0.69` for split-by-time vs `0.77` for random split. (in exp/readme , exp/src/splitbytime.ipynb)